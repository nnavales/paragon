local M = {}

-- Simple color blending for foreground
function M.blend_fg(color, amount)
  -- For now, just return the original color
  -- You can implement proper blending later
  return color
end

-- Simple color blending for background  
function M.blend_bg(color, amount)
  -- For now, just return the original color
  -- You can implement proper blending later
  return color
end

return M